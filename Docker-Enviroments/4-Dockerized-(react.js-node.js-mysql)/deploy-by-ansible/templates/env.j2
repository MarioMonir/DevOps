# Node envs --------------------------------
NODE_ENV={{ NODE_ENV }}
PROJECT_NAME={{ PROJECT_NAME }}

# API  -------------------------------------
DOMAIN=http://localhost
PORT={{ PROJECT_API_PORT }}
BASE_URL=http://localhost:${PORT}

# Client  -----------------------------------
CLIENT_PORT={{ PROJECT_CLIENT_PORT}}
CLIENT_URL="http://localhost:${CLIENT_PORT}"


# Databasse envs ----------------------------
DB_HOST=mario_mysqldb
DB_NAME=${PROJECT_NAME}
DB_PORT=3306
DB_USER=root
DB_PASSWORD=${PROJECT_NAME}
DB_DIALECT=mysql
DB_CONNECTION_LIMIT=5
DATABASE_URL=${DB_DIALECT}://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?connection_limit=${DB_CONNECTION_LIMIT}